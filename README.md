# practice_2.4

Каждое задание вынесено в отдельный файл с названием вида `task_x.py`.

## Запуск

```bash
python task_1.py
```

Если в папке есть файл `requirements.txt`, перед запуском выполните:

```bash
pip install -r requirements.txt
```
